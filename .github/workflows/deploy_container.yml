name: Build and Deploy Container

on:
  workflow_dispatch:

jobs:
  build:
    name: Build and Push .NET API Container
    runs-on: ubuntu-latest

    steps:
      - name: Checkout API Source
        uses: actions/checkout@v4

      - name: Set up .NET
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: '8.0.x'

      - name: Restore Dependencies
        run: dotnet restore

      - name: Build and Publish API
        run: dotnet publish KejaHunT_Gateway.csproj -c Release -o ./publish

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_TOKEN }}

      - name: Build and Push Docker Image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: kalukwo/kejahunt-gateway:latest

#   trigger-remote:
#     name: Trigger Remote Deployment
#     needs: build
#     runs-on: ubuntu-latest

#     steps:
#       - name: Trigger remote workflow
#         run: |
#           curl -X POST \
#             -H "Authorization: Bearer ${{ secrets.GCP_TRIGGER_TOKEN }}" \
#             -H "Accept: application/vnd.github.v3+json" \
#             https://api.github.com/repos/Kalukwo/KejaHUnTinfrastructure/actions/workflows/deploy.yml/dispatches \
#             -d '{"ref":"main"}'